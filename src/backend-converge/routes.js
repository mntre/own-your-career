/**
 * Own Your Career — API Routes (Converge Cloud)
 * 
 * REST endpoints for all portal operations.
 * All routes require authentication and RBAC validation.
 * 
 * @fileoverview Express route definitions
 */

'use strict';

// TODO: Define REST endpoints:
// POST /api/skills-assessment       (Step 1)
// POST /api/okr-upload              (Step 2)
// POST /api/self-assessment         (Step 3)
// POST /api/feed-forward            (Step 4)
// POST /api/acknowledgement         (Steps 5 & 7)
// GET  /api/workflow-status/:empId   (Gate checking)
// GET  /api/scores/:empId            (Step 6 - read only)
// GET  /api/team/:managerId          (Manager team list)
// GET  /api/org-data/:spocId         (Data SPOC hierarchy)
